-title "Schedules"

= form_tag schedules_path, :method => 'get', :id => 'schedule_search' do
  = text_field_tag :search, params[:search]
  = submit_tag "Search", :name => nil

%ul
  - for schedule in @schedules
    %li
      %strong
        =link_to schedule.name, schedule
      .date
        =schedule.date.strftime('%d. %b, %Y') #format noch falsch gleich über i18 richtig machen
      .count
        =pluralize(schedule.comments.count, 'Comment')

  - if user_signed_in?
    %li
      = link_to 'New Schedule', new_schedule_path

