-title "Schedules"
%ul
  - for schedule in @schedules
    %li
      %strong
        =schedule.name
      %em
        =schedule.created_at.strftime('%d. %b, %Y') #format noch falsch gleich über i18 richtig machen
      = link_to 'show', schedule

  - if user_signed_in?
    %li
      = link_to 'New Schedule', new_schedule_path

= render :partial => "tag_cloud"

