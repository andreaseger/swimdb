%fieldset.item
  =# f.fields_for 'new_items', :index =>nil do |ff|
  %ul.horizontal
    %li= f.number_field :level, :in => 0..2, :step => 1, :id => 'level', :class => 'item-level', :size => 10
    %li= f.text_field :text,                             :id => 'text',  :class => 'item-text',  :size => 40
    %li= remove_child_link "(remove)", f

